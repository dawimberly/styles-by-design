CONTRACTOR_PIN=420420
# Styles by Design Stripe account only — do not reuse Paq'in Family House (dirty-ink) keys.
STRIPE_SECRET_KEY=
# Webhook signing secret from Workbench → Webhooks (whsec_...). Not an API key.
STRIPE_WEBHOOK_SECRET=
# Prefer a restricted key (rk_) over a secret key (sk_). Store these as Vercel sensitive env vars.
# Candidate tax codes from https://docs.stripe.com/tax/tax-codes — confirm with a tax advisor.
STRIPE_CABINET_TAX_CODE=txcd_99999999
STRIPE_DESIGN_TAX_CODE=txcd_20060022
CTG_ORDER_EMAIL=divya@durastoneusa.com
EMPLOYEE_PIN=420420
